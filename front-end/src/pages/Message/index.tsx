import React, { useEffect } from "react";
import TwoPanelLayout from "./layout";

type Props = {};

const MessageSection = (props: Props) => {
    useEffect(() => { 
        
    })
  return (
    <div className="h-100">
      <TwoPanelLayout />
    </div>
  );
};

export default MessageSection;
